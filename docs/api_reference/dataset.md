# `DatasetConsumer` and `DatasetProducer` classes

The `DatasetConsumer` and `DatasetProducer` classes are used to interact with `Kafka` topics.

::: aineko.DatasetConsumer

::: aineko.DatasetProducer
