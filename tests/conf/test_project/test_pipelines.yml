test_run_1:
  test_params:
    timeout: 30 # timeout in seconds

  patch_nodes:
    sequencer_patch:
      class: aineko.tests.test.pipeline_test.TestSequencerPatch
      node_to_patch: sequencer # node name to patch (maps to pipeline config)
      outputs:
        - integer_sequence

  checker_nodes:
    doubler_checker:
      class: aineko.tests.test.pipeline_test.TestDoublerChecker
      inputs:
        - integer_doubles
